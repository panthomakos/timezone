version: "2"         # required to adjust maintainability checks

checks:
  method-complexity:
    enabled: true
    config:
      threshold: 15
  return-statements:
    enabled: true
    config:
      threshold: 10
